import * as NavCard from "../../_components/NavCard";

export const FAQNavCard = (props: NavCard.PageNavCardProps) => {
  return <NavCard.Card {...props} title="FAQ" description="Frequent questions" externalUrl="#" arrowPosition="right" />;
};
